ai_context_version: 1.0

project:
  name: chronik
  summary: UI/Dashboard für das Heimgewebe (vormals leitstand)
  role: observability_control_room
  primary_language: typescript
  visibility: internal

dependencies:
  internal:
    - name: semantah
      relationship: uses
      interface:
        - graph_query
        - insight_stream
    - name: hauski
      relationship: uses
      interface:
        - trace_view
        - action_log
  external:
    - name: React
    - name: Vite
