class: rust-service

tasks:
  smoke: |
    # schneller Grundcheck: baut das Projekt?
    cargo build --workspace

  guard: |
    # Formatierung und Clippy hart durchziehen
    cargo fmt --all --check
    cargo clippy --all-targets --all-features -- -D warnings

  metrics: ""
  snapshot: ""
